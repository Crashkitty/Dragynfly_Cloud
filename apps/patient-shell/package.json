{
  "name": "@dragonfly/patient-shell",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Capacitor wrapper around apps/patient-pwa. Hosts the iOS and Android shells that ship the @dragonfly/capacitor-sensor-bridge plugin so window.DragonflyBridge works on real devices.",
  "scripts": {
    "build:web": "npm run build --workspace=@dragonfly/patient-pwa",
    "sync": "npm run build:web && cap sync",
    "open:ios": "cap open ios",
    "open:android": "cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.0",
    "@capacitor/core": "^6.1.0",
    "@capacitor/ios": "^6.1.0",
    "@dragonfly/capacitor-sensor-bridge": "*"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0"
  }
}
