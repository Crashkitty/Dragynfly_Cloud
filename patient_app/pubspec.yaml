name: dragonfly_patient
description: Dragonfly Cloud — Diabetes Taiyi Intervention Pilot (patient app)
publish_to: none
version: 0.1.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1
  go_router: ^14.6.0
  flutter_secure_storage: ^9.2.2
  local_auth: ^2.3.0
  fl_chart: ^0.69.0
  image_picker: ^1.1.2
  intl: ^0.19.0
  uuid: ^4.5.1
  shared_preferences: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
